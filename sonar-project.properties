# Required metadata
sonar.projectKey=rest-framework-t4
sonar.projectName=Simplyfing REST development for enterprises
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=.

# Language
sonar.language=cs

# Encoding of the source files
sonar.sourceEncoding=UTF-8
